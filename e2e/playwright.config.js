const config = {
    use: {
        baseURL: 'http://localhost:8080',
        extraHTTPHeaders: {
            'Content-type' : 'application/json; charset=UTF-8'
        }
    }
}

module.exports = config